# Core requirements to run the website
Django>=4.2,<5.0
python-dotenv>=1.0.0
pypdf>=3.0.0
anthropic>=0.34.0

# Production server + deployment (Render, Vercel, etc.)
gunicorn>=21.2.0
whitenoise>=6.6.0
dj-database-url>=2.1.0
psycopg2-binary>=2.9.9
django-storages>=1.14
boto3>=1.34

# Optional: only needed if you switch DATABASES in project/settings.py
# to MySQL instead of Postgres/SQLite
# mysqlclient>=2.2.0

# Optional: only needed for the model-training/evaluation scripts
# (baseline_model.py, evaluate.py), not for running the website itself
# pandas>=2.0.0
# scikit-learn>=1.4.0
# scipy>=1.11.0
